version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - gocritic
    - misspell
    - unconvert
    - unparam
  exclusions:
    generated: strict
    presets:
      - common-false-positives
      - std-error-handling

formatters:
  enable:
    - gofmt
