# Agents

Before marking any work complete, run:

```plaintext
make lint
make test
make build
```

Git commits must use conventional commit messages (e.g. `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`, `ci:`).
